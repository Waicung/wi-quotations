<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WI QUOTATIONS</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto p-4">
        <div class="bg-white shadow-md rounded-lg p-6 mb-6">
            <h1 class="text-3xl font-bold mb-4">Generate Quotation</h1>
            <form action="/generate-quotation" method="post">
                <div class="mb-4">
                    <label for="issuer" class="block text-sm font-medium text-gray-700">Issuer</label>
                    <select id="issuer" name="issuer" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        <!-- Options for issuer -->
                        {% for profile in profiles %}
                            <option value="{{ profile.id }}">{{ profile.label }}</option>
                        {% endfor %} 
                    </select>
                </div>
                <div class="mb-4">
                    <label for="client" class="block text-sm font-medium text-gray-700">Client</label>
                    <select id="client" name="client" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        <!-- Options for client -->
                        {% for profile in profiles %}
                        <option value="{{ profile.id }}">{{ profile.label }}</option>
                        {% endfor %} 
                    </select>
                </div>
                <div class="mb-4">
                    <label for="amount" class="block text-sm font-medium text-gray-700">Amount</label>
                    <input type="text" id="amount" name="amount" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                </div>
                <button type="submit" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-700">Generate Quotation</button>
            </form>
        </div>
        <div class="bg-white shadow-md rounded-lg p-6">
            <h1 class="text-3xl font-bold mb-4">Generate Invoice</h1>
            <form action="/generate-invoice" method="post">
                <div class="mb-4">
                    <label for="issuer" class="block text-sm font-medium text-gray-700">Issuer</label>
                    <select id="issuer" name="issuer" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        <!-- Options for issuer -->
                        {% for profile in profiles %}
                        <option value="{{ profile.id }}">{{ profile.label }}</option>
                        {% endfor %} 
                    </select>
                </div>
                <div class="mb-4">
                    <label for="client" class="block text-sm font-medium text-gray-700">Client</label>
                    <select id="client" name="client" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        <!-- Options for client -->
                        {% for profile in profiles %}
                        <option value="{{ profile.id }}">{{ profile.label }}</option>
                        {% endfor %} 
                    </select>
                </div>
                <div class="mb-4">
                    <label for="amount" class="block text-sm font-medium text-gray-700">Amount</label>
                    <input type="text" id="amount" name="amount" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                </div>
                <button type="submit" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-700">Generate Invoice</button>
            </form>
        </div>
    </div>
</body>
</html>